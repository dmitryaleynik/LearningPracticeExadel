;!function(AddNewArticleView) {
    "use strict";

    class NewArticleInitialization {

        init() {
            return new AddNewArticleView().render();
        }

    }

    window.NewArticleInitialization = NewArticleInitialization;
}(window.AddNewArticleView);